this folder contains windows binaries of FFMPEG that are required to build SlitScanGenerator.
These were downloaded from the official webpage https://www.ffmpeg.org/.

Extract the contents of the ZIP archives into subdirectories here:
    .\win32\
    .\win64\
These then should have the appropriate subdirs bin, include, lib, ...
  